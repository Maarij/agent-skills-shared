# [Project Name]

[One paragraph: what the project is and what its deliverable is — the thing whose correctness matters most. Domain language here is shared by UI copy, the code, and PRDs — keep them in sync.]

## Language

<!--
The glossary grows lazily — /grill-with-docs adds entries as terms get resolved in
grilling sessions. Don't pre-fill it with guesses. Each entry follows this shape:

**Term**:
Precise definition, including how it maps to code (which function/index/field) and how
it surfaces in UI copy (the exact string, if any). Link the ADR or PRD that fixed it.
_Surfaced as_: the user-facing string, if it differs from the canonical term.
_Avoid_: the synonyms this term exists to displace — the words that caused, or would
cause, confusion. This line is what makes an entry load-bearing.

Group entries under thematic headings (### Time axis, ### Money, …) once there are
enough to need them.
-->

### [First thematic group]

**[First term]**:
[Definition.]
_Avoid_: [synonyms].
